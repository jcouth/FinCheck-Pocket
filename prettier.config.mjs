const config = {
  printWidth: 80,
  tabWidth: 2,
  useTabs: false,
  singleQuote: true,
  arrowParens: 'avoid',
  endOfLine: 'lf',
  plugins: ['prettier-plugin-tailwindcss'],
};

export default config;
